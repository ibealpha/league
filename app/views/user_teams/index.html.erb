<h1>Listing Teams</h1>
 
<table>
  <tr>
    <th>Name</th>
  </tr>
 
  <% @user_teams.each do |team| %>
    <tr>
      <td><%= link_to team.name, user_team_path(team) %></td>
    </tr>
  <% end %>
</table>